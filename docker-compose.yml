version: '3.9'

services:
  gca-be:
    build:
      context: .
      dockerfile: Dockerfile
    image: gca-be:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    # volumes:
    #   - ./logs:/app/logs

# Nếu cần database nội bộ, mở giống mẫu dưới:
#  db:
#    image: postgres:16-alpine
#    environment:
#      POSTGRES_USER: ${POSTGRES_USER}
#      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#      POSTGRES_DB: ${POSTGRES_DB}
#    volumes:
#      - db_data:/var/lib/postgresql/data
#    ports:
#      - "5432:5432"

#volumes:
#  db_data:
